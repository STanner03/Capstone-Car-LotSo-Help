import useAuth from "../../hooks/useAuth";

const FillupPage = ({ activeVehicle, vehicleFillups }) => {
    // State Variables:
    const [user, token] = useAuth();

  return <div>Hello World</div>;
};

export default FillupPage;
